<select id="sortListings" {{action 'sortListingChange' on='change'}}>
  <option value="date:desc">Date Descending</option>
  <option value="date">Date Ascending</option>
</select>

{{#each sortedListings as |listing|}}
  {{listing-tile listing=listing}}
{{/each}}
